
---

- hosts: all
  tasks:

  - name: install apache2 package
    dnf:
     name: httpd
     state: present

  - name: Start and enable Apache server
    service:
     name: httpd
     state: started
     enabled: true 
